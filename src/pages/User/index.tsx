import React from 'react';
import Menu from '../../components/Menu';

import { Container } from './styles';

const User: React.FC = () => {
  return (
    <Container>
      <Menu />
    </Container>
  );
};

export default User;
